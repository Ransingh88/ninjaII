import './counter.css'

export function Counter({passedCounter}) {
     //const counter = 20;

    return <div className="redText">
            <h1>Counter value is :{passedCounter}</h1>
        </div>
    
}

